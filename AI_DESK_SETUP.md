# LEVEL UP AI Desk

The AI Finance Desk uses the secure `/api/ai` route for model responses. Private credentials remain server-side.

## API contract

`POST /api/ai`

Request:
`{"message":"Explain RPA in accounting"}`

Response:
`{"answer":"..."}`
